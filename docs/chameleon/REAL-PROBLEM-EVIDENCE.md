# Real Problem Evidence

> **CI-gated.** Tagging `v1.0.0` (or any semver-stable release) requires
> this file to contain ≥ 5 H2 sections matching the transcript schema below.
> Build fails otherwise.

Per ARCHITECTURE.md "Real Problem Evidence" — empty until EF dogfood
collects concrete transcripts demonstrating chameleon's value.

## Working hypothesis (v0.x acceptance)

AI-generated code in established codebases routinely violates local
conventions in ways that cost reviewer time but don't affect correctness.
Hypothesis is supported by:

- Active development of `claude-measure-twice` (predecessor) as one team's
  response
- The `CLAUDE.md` convention adoption rate across Claude Code users
- Anecdotal reports from author's day-to-day work at Empire Flippers

## Required evidence before v1.0.0

≥ 5 transcripts, each in the schema below.

### Transcript schema

```markdown
## Transcript N: <one-line summary>

**Repo**: ef-api / ef-client / other
**Date**: YYYY-MM-DD
**Stack**: Ruby on Rails / TypeScript / ...
**Engineer**: Cris / <name>
**PR**: <link to the PR where the off-pattern code was caught>

### Without chameleon

Generated by: Claude (no chameleon profile active)
Prompt: <the actual user prompt>

```<lang>
<the code Claude generated>
```

### Reviewer feedback

Reviewer: <name>
Feedback: <what they flagged — paste the comment>
Time-to-fix: <minutes from comment to merge>

### What chameleon should have produced

```<lang>
<the convention-correct code>
```

### Why this matters

<one to two sentences on the cost: review cycles, fix commits,
mental model drift, etc.>

### Mapping to chameleon dimensions

- Tier (1 / 2 / 3): which dimension would have caught this?
- Specific catch: <archetype / canonical / idiom that applies>
```

## Status

**Empty.** Phase 5 (EF dogfood) populates this. Owner: Cris.

## Why this matters

Per Round 1 Jesse Vincent verification: "the architecture asserts a problem
without showing it. Where are the transcripts? What edit went wrong, in
which file, costing what rework? Without that, this is a speculative fix."

Round 5 Engineering Manager echoed: success metrics must be falsifiable.
The CI gate makes Phase 5 dogfood non-skippable.

If insufficient evidence emerges during dogfooding, the plugin's value
proposition must be revisited honestly rather than shipped on speculation.
