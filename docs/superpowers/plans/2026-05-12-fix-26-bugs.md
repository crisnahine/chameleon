# Fix 26 Dogfood Bugs Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Land fixes for the 26 bugs documented in the May 12 dogfood run (`Testing Apps/_chameleon_test_results/_BUGS.md`). Ship as v0.5.6.

**Architecture:** One commit per bug, on `main`. Each fix is TDD: test → fail → implement → pass → commit. No AI attribution in any commit message per global rule.

**Tech Stack:** Python 3 (FastMCP), tree-sitter (TS), Prism (Ruby), SQLite.

---

## Phase 1: Quick wins (lines-of-code-small)

### Task 1: BUG-005 — `trust_state: "n/a"` when there is no profile

**Files:**
- Modify: `mcp/chameleon_mcp/tools.py:322-329`
- Test: `tests/v0_5_6_trust_state_test.py` (create)

- [ ] **Step 1: Write failing test**

```python
# tests/v0_5_6_trust_state_test.py
import json, subprocess, tempfile, pathlib
from chameleon_mcp.tools import detect_repo

def test_no_profile_returns_na_trust_state(tmp_path):
    pkg = tmp_path / "package.json"
    pkg.write_text('{"name":"x","dependencies":{"typescript":"5"}}')
    (tmp_path / "tsconfig.json").write_text("{}")
    sample = tmp_path / "a.ts"; sample.write_text("export const x = 1;")
    resp = detect_repo(str(sample))
    assert resp["data"]["profile_status"] == "no_profile"
    assert resp["data"]["trust_state"] == "n/a", f"got {resp!r}"
```

- [ ] **Step 2: Implement**

In `tools.py` change the trust_state derivation:

```python
if not profile_present:
    trust_state = "n/a"
elif trust is None:
    trust_state = "untrusted"
elif is_material_change(repo_id, profile_dir):
    trust_state = "stale"
else:
    trust_state = "trusted"
```

- [ ] **Step 3: Test passes, then commit**

```
Fix BUG-005: trust_state is "n/a" when no profile present

Was returning "untrusted" even when no profile existed, implying a
trustable thing was lurking. Schema docstring already says "n/a" is
the right value when there is no profile.
```

---

### Task 2: BUG-007 — Engine version frozen at 0.4.0 in profile

**Files:**
- Modify: `mcp/chameleon_mcp/bootstrap/orchestrator.py:260`
- Test: `tests/v0_5_6_engine_version_test.py`

- [ ] **Step 1: Failing test**

```python
import json, pathlib, tomllib
def test_engine_version_matches_pyproject(tmp_path):
    from chameleon_mcp.bootstrap.orchestrator import ENGINE_MIN_VERSION
    pyproject = pathlib.Path(__file__).resolve().parents[1] / "mcp" / "pyproject.toml"
    data = tomllib.loads(pyproject.read_text())
    pkg_version = data["project"]["version"]
    assert ENGINE_MIN_VERSION == pkg_version
```

- [ ] **Step 2: Implement**

Replace `ENGINE_MIN_VERSION = "0.4.0"` with version read at import time from the package metadata:

```python
try:
    from importlib.metadata import version as _pkg_version
    ENGINE_MIN_VERSION = _pkg_version("chameleon-mcp")
except Exception:  # pragma: no cover
    ENGINE_MIN_VERSION = "0.5.6"
```

- [ ] **Step 3: Commit**

```
Fix BUG-007: ENGINE_MIN_VERSION reads from package metadata

Was hardcoded to "0.4.0"; profile.summary.md and profile.json carried
that wrong value forever. Now reads from importlib.metadata at import.
```

---

### Task 3: BUG-006 — `propose_archetype_renames` includes current name in candidates

**Files:**
- Modify: `mcp/chameleon_mcp/bootstrap/naming.py` (find `_propose_alternatives_for`)
- Test: `tests/v0_5_6_rename_candidates_test.py`

- [ ] **Step 1: Failing test**

```python
def test_current_name_not_in_alternatives():
    from chameleon_mcp.bootstrap.naming import _propose_alternatives_for_archetype
    alts = _propose_alternatives_for_archetype(
        current_name="cluster-a2cfb565",
        canonical_path="src/testing/mocks/handlers/comments.ts",
        paths_pattern="src/mocks/handlers:ts",
        top_level_node_kinds=["ImportDeclaration"],
    )
    assert "cluster-a2cfb565" not in alts
    assert all("cluster-a2cfb565" not in a for a in alts), alts
```

- [ ] **Step 2: Implement: filter out the current_name and any name containing it from alternatives, dedupe**

- [ ] **Step 3: Commit:** `Fix BUG-006: rename candidates exclude current name`

---

### Task 4: BUG-026 — `bootstrap_repo` overwrites existing profile without confirmation

**Files:**
- Modify: `mcp/chameleon_mcp/tools.py` (bootstrap_repo entry)

- [ ] **Step 1: Failing test:** call bootstrap twice on the same path; second call should return `status: "already_bootstrapped"` unless `force=True`.

- [ ] **Step 2: Implement:** add `force: bool = False` param. Before bootstrap, check `profile_dir / "COMMITTED"`; if present and not force, return early with `already_bootstrapped` status carrying the existing profile metadata.

- [ ] **Step 3: Commit:** `Fix BUG-026: bootstrap_repo requires force=true to overwrite`

---

### Task 5: BUG-022 — `get_pattern_context` response shape inconsistent on corrupted-profile path

**Files:**
- Modify: `mcp/chameleon_mcp/tools.py` (get_pattern_context, look for the corrupted-profile early-return)

- [ ] **Step 1: Failing test:** corrupt profile.json, call get_pattern_context, assert response shape matches healthy shape (`archetype.archetype`, `content_signal_match`, `idioms` field present).

- [ ] **Step 2: Implement:** factor the response shape into a helper and use it on both paths.

- [ ] **Step 3: Commit:** `Fix BUG-022: get_pattern_context corrupted-profile shape matches healthy`

---

### Task 6: BUG-023 — Schema version mismatch silently accepted

**Files:**
- Modify: `mcp/chameleon_mcp/profile/loader.py` (load_profile_dir)

- [ ] **Step 1: Failing test:** write a profile.json with `schema_version: 99`; load_profile_dir should raise / return a clear error.

- [ ] **Step 2: Implement:** if `schema_version > PROFILE_SCHEMA_VERSION` raise `ProfileSchemaTooNewError` with a helpful message ("upgrade chameleon to read this profile").

- [ ] **Step 3: Commit:** `Fix BUG-023: refuse to load profile with newer schema_version`

---

### Task 7: BUG-013 — Numeric-suffix archetype names

**Files:**
- Modify: `mcp/chameleon_mcp/bootstrap/naming.py` (collision resolution)

- [ ] **Step 1: Failing test:** Two clusters with `react-component` base name and different paths (`src/components/button` vs `src/components/icons`) should produce `react-component-button` and `react-component-icons`, not `react-component-1`/`react-component-2`.

- [ ] **Step 2: Implement:** when a name collides, derive the disambiguator from the canonical file's directory tail (last 1-2 path segments) instead of a counter. Fall back to a counter only when path segments are identical.

- [ ] **Step 3: Commit:** `Fix BUG-013: rename collisions use path segment, not counter`

---

### Task 8: BUG-012 — Two different statuses for "no source files"

**Files:**
- Modify: `mcp/chameleon_mcp/bootstrap/orchestrator.py` (status return when zero matching files)

- [ ] **Step 1: Failing test:** call bootstrap on a dir with `package.json` containing TS deps but ZERO .ts files. Expected status: `failed_unsupported_language` (unify on that single status).

- [ ] **Step 2: Implement:** change the "No TypeScript files found matching the discovery glob" branch to return `failed_unsupported_language` with the original error appended.

- [ ] **Step 3: Commit:** `Fix BUG-012: unify "no source files" status as failed_unsupported_language`

---

## Phase 2: Medium API/UX

### Task 9: BUG-004 — `trust_profile` accepts repo_id OR path

**Files:** `mcp/chameleon_mcp/tools.py` (trust_profile entry).

- [ ] **Step 1: Failing test:** call trust_profile with a repo_id; should succeed (currently fails with "repo path must be absolute").

- [ ] **Step 2: Implement:** at the top of trust_profile, if `repo` looks like a 64-hex repo_id (regex match), look up the path via `index_db.get_repo_root(repo)` and proceed with that path. Otherwise keep the existing path validation.

- [ ] **Step 3: Commit:** `Fix BUG-004: trust_profile accepts repo_id or absolute path`

---

### Task 10: BUG-021 — Corrupted profile reported as `profile_corrupted`

**Files:** `mcp/chameleon_mcp/tools.py` (detect_repo), `mcp/chameleon_mcp/profile/loader.py`.

- [ ] **Step 1: Failing test:** corrupt profile.json. detect_repo should return `profile_status: "profile_corrupted"`.

- [ ] **Step 2: Implement:** detect_repo opens profile.json with json.load; on ValueError return the new status with `trust_state: "n/a"`.

- [ ] **Step 3: Commit:** `Fix BUG-021: detect_repo distinguishes profile_corrupted`

---

### Task 11: BUG-010 — `excalidraw-app`-style workspace detection (no own tsconfig)

**Files:** `mcp/chameleon_mcp/bootstrap/orchestrator.py` (`_select_extractor` / signal check).

- [ ] **Step 1: Failing test:** workspace with `package.json` lacking TS deps but containing 5 .ts files in subdirs, root has tsconfig.json. Bootstrap should succeed.

- [ ] **Step 2: Implement:** add a third TS signal: presence of `>= 1` `*.ts`/`*.tsx` file via a bounded shallow scan (depth 3, capped at 50 found).

- [ ] **Step 3: Commit:** `Fix BUG-010: workspace TS detection falls back to .ts/.tsx presence`

---

### Task 12: BUG-011 — Top-level `archetypes_detected` sums across workspaces

**Files:** `mcp/chameleon_mcp/bootstrap/orchestrator.py` (workspace fanout report assembly).

- [ ] **Step 1: Failing test:** bootstrap a workspace repo where workspace A has 2 archetypes and workspace B has 3. Top-level `archetypes_detected` should be 5, with a new `archetypes_per_workspace` map.

- [ ] **Step 2: Implement:** sum across workspaces; add per-workspace map.

- [ ] **Step 3: Commit:** `Fix BUG-011: top-level archetypes_detected sums across workspaces`

---

### Task 13: BUG-015 — Path-prefix fallback for non-canonical Rails files

**Files:** `mcp/chameleon_mcp/tools.py` (get_archetype / archetype matching).

- [ ] **Step 1: Failing test:** A profile with archetype `controller` keyed by `paths_pattern=app/controllers/v1`. File `app/controllers/application_controller.rb` should match `controller` with `confidence_band: "low"` (fallback), not `archetype: null`.

- [ ] **Step 2: Implement:** if no exact paths_pattern match, find the archetype with the longest shared path prefix + same extension + return `confidence_band: "low"` with the archetype id.

- [ ] **Step 3: Commit:** `Fix BUG-015: archetype fallback by longest path-prefix overlap`

---

### Task 14: BUG-017 — `language_hint` populated on misdetection path too

**Files:** `mcp/chameleon_mcp/bootstrap/orchestrator.py` (envelope construction).

- [ ] **Step 1: Failing test:** a Rails-with-frontend repo where primary detection picks TS by mistake. Result should still surface `language_hint.primary_detected=ruby, but bootstrap=typescript`.

- [ ] **Step 2: Implement:** always compute the hint; populate when EITHER detection picks one and the other has a meaningful sidecar (file count threshold).

- [ ] **Step 3: Commit:** `Fix BUG-017: language_hint surfaces even on possibly-wrong primary detection`

---

### Task 15: BUG-018 — Cache hygiene: prune old plugin versions

**Files:** Create `scripts/prune-plugin-cache.sh`. Document in `README.md` + `CLAUDE.md`.

- [ ] **Step 1:** Write a script that lists `~/.claude/plugins/cache/chameleon/chameleon/*` and removes all but the version named in `installed_plugins.json`.

- [ ] **Step 2:** Add a "post-install" note to `README.md`: after upgrading chameleon, restart Claude Code to pick up the new MCP, then run the prune script.

- [ ] **Step 3: Commit:** `Fix BUG-018: ship prune-plugin-cache script and upgrade note`

---

### Task 16: BUG-024 — Preflight hook gates injection on trust state

**Files:** `hooks/preflight-and-advise`.

- [ ] **Step 1: Failing test:** call the hook script on an untrusted repo. Expected: surface trust prompt + suppress canonical injection until trusted.

- [ ] **Step 2: Implement:** read trust_state from MCP detect_repo result; when "untrusted", emit a one-time trust-prompt context (track via marker file `~/.local/share/chameleon/<repo_id>/.trust_prompted.<session>`) and skip canonical injection.

- [ ] **Step 3: Commit:** `Fix BUG-024: preflight gates canonical injection on trust_state`

---

### Task 17: BUG-025 — Trim `/chameleon-status` skill to features that exist

**Files:** `skills/chameleon-status/SKILL.md`.

- [ ] **Step 1:** Edit the skill to:
  - Keep: Profile summary, archetypes list, trust state, days_since_refresh, observed_drift_score, recommended_action
  - Remove: "MCP errors (last 24h)", "p99 hook latency", "edits matching archetype last 30 sessions"
  - Move removed items to a "Future work (Phase 4)" section at the bottom

- [ ] **Step 2: Commit:** `Fix BUG-025: chameleon-status skill trimmed to existing MCP coverage`

---

### Task 18: BUG-019 — Sidecar bootstrap inherits parent signals (or change suggestion)

**Files:** `mcp/chameleon_mcp/bootstrap/orchestrator.py` (path resolution at entry).

- [ ] **Step 1: Failing test:** call bootstrap on `<rails-repo>/app/javascript` (no own package.json). Should succeed using parent's signals when discoverable.

- [ ] **Step 2: Implement:** if signals not found at `path`, walk up to git root or repo root marker (existing find_repo_root logic) and use that as the signal source; record an `inherited_from` field in the envelope.

- [ ] **Step 3: Commit:** `Fix BUG-019: sidecar bootstrap inherits parent signals`

---

## Phase 3: Extractors

### Task 19: BUG-003 — `.eslintrc.cjs` parser

**Files:** `mcp/chameleon_mcp/bootstrap/tool_config.py` (eslint extractor).

- [ ] **Step 1: Failing test:** fixture `tests/fixtures/eslintrc_cjs/.eslintrc.cjs` containing `module.exports = { rules: { 'no-console': 'warn' } }`. Extract should return that rules dict.

- [ ] **Step 2: Implement:** shell out to `node -e "console.log(JSON.stringify(require('<path>')))"` (with a 2s timeout). If node not available, fall back to a tree-sitter walk for the `module.exports = { ... }` literal.

- [ ] **Step 3: Commit:** `Fix BUG-003: parse .eslintrc.cjs via node require with tree-sitter fallback`

---

### Task 20: BUG-020 — `eslint.config.mjs` (flat config) parser

**Files:** `mcp/chameleon_mcp/bootstrap/tool_config.py`.

- [ ] **Step 1: Failing test:** fixture `tests/fixtures/eslint_flat/eslint.config.mjs` containing `export default [{ rules: { 'no-console': 'warn' } }]`. Extract should return the flattened rules dict.

- [ ] **Step 2: Implement:** detect filename `eslint.config.{js,mjs,cjs,ts}`. Use node ESM `import('<file>')` if available, otherwise tree-sitter to find `export default [...]` literal.

- [ ] **Step 3: Commit:** `Fix BUG-020: parse eslint.config.mjs flat config`

---

### Task 21: BUG-014 — Rubocop extractor

**Files:** Create `mcp/chameleon_mcp/bootstrap/rubocop.py`; wire into `tool_config.py`.

- [ ] **Step 1: Failing test:** fixture `.rubocop.yml` with cops. Extract returns the cops dict and plugin names.

- [ ] **Step 2: Implement:** YAML load, capture top-level keys, plugin list, and AllCops settings. Surface as `rules.rubocop` in the profile.

- [ ] **Step 3: Commit:** `Fix BUG-014: rubocop extractor reads .rubocop.yml`

---

## Phase 4: Critical impact

### Task 22: BUG-008 / BUG-009 — Cap and aggregate `sparse_cluster_warnings`

**Files:** `mcp/chameleon_mcp/bootstrap/orchestrator.py` (warning assembly).

- [ ] **Step 1: Failing test:** Bootstrap a synthetic repo with 200 singleton clusters. Response `sparse_cluster_warnings` length must be ≤ 50 AND a `sparse_cluster_warnings_truncated: true, total_sparse_clusters: 200` envelope must be present.

- [ ] **Step 2: Implement:**
  - Aggregate warnings by `paths_pattern` first (so 4 size-1 clusters at `src/x/y:ts` collapse to one row with `member_count: 4`).
  - After aggregation, truncate at 50 with the truncation flag.

- [ ] **Step 3: Commit:** `Fix BUG-008/009: aggregate + cap sparse_cluster_warnings`

---

### Task 23: BUG-002 — Permissive clustering tier

**Files:** `mcp/chameleon_mcp/bootstrap/clustering.py`.

- [ ] **Step 1: Failing test:** clusterer fed 5 files at `src/comments/api:ts` that differ in AST top-level node kinds by 1-2 nodes each. Today: 5 size-1 clusters. After fix: 1 cluster of 5 OR a "loose" cluster of 5 with `tier: "loose"`.

- [ ] **Step 2: Implement:** add a second-pass loose-merge step: any group of singletons sharing the same `paths_pattern` that also have AST shape Jaccard ≥ 0.5 merge into one cluster with `cluster_tier: "loose"` (default tier is `"tight"`).

- [ ] **Step 3: Commit:** `Fix BUG-002: add loose-merge clustering tier for sparse archetypes`

---

### Task 24: BUG-001 — Ad-hoc monorepo discovery hint

**Files:** `mcp/chameleon_mcp/bootstrap/orchestrator.py` (top of bootstrap).

- [ ] **Step 1: Failing test:** call bootstrap on a dir with no own TS deps but `apps/*/package.json` containing TS deps. Result: `failed_unsupported_language` + a new `discovery_hints: [{ subdir: "apps/nextjs-app", ... }]` field listing the discoverable sub-apps.

- [ ] **Step 2: Implement:** on `failed_unsupported_language`, do a shallow walk of `apps/*` and `packages/*`; if any contain package.json with TS deps, list them under `discovery_hints`.

- [ ] **Step 3: Commit:** `Fix BUG-001: bootstrap surfaces discovery_hints for ad-hoc monorepos`

---

### Task 25: BUG-016 — Verify gitlabhq is fixed in v0.5.5

**Files:** check `mcp/chameleon_mcp/bootstrap/orchestrator.py:_is_rails_with_frontend`.

- [ ] **Step 1: Verify the function recognizes all three paths** (`app/javascript`, `app/assets/javascripts`, `app/frontend`).
- [ ] **Step 2:** If yes, write a regression test that fakes a repo with `app/assets/javascripts/foo.js` + `Gemfile` + `package.json` and asserts the Ruby extractor wins.
- [ ] **Step 3:** Note in `RELEASE-NOTES.md` that gitlabhq detection requires v0.5.6+ (or v0.5.3+ if the v0.5.3 fix is sufficient — verify and report).
- [ ] **Step 4: Commit:** `Fix BUG-016: gitlabhq Rails-with-frontend detection regression test`

---

## Phase 5: Ship

### Task 26: Bump version to 0.5.6 and tag

- [ ] **Step 1:** Run `scripts/bump-version.sh 0.5.6`.
- [ ] **Step 2:** Update `RELEASE-NOTES.md` with a digest of all 26 fixes.
- [ ] **Step 3:** Run the full test suite: `cd mcp && PYTHONPATH=.:../tests .venv/bin/python ../tests/run_all_orders.py`.
- [ ] **Step 4: Commit and tag:** `Ship v0.5.6: 26 dogfood bug fixes`.

---

## Spec coverage check

26 distinct BUG IDs → 25 tasks (one per bug) + 1 release task. BUG-008 and BUG-009 collapsed into Task 22 (same fix). BUG-018 is partly docs + script (Task 15).

## No placeholders

Each task has a concrete file, a failing-test sketch, the implementation direction, and the commit message. Where code is substantial (Task 22, 23) the implementation details are in prose because the actual diff is large enough to author live.

