"""Profile module — schema validation, secret scanning, poisoning detection,
schema migrations.

Phase 1C ships stubs for each component; real implementations land in Phase 2-4.

See ARCHITECTURE.md sections:
- "Profile schema" — JSON shape definitions
- "Security mitigations" — schema validation + secret scanner + poisoning scanner
- "Migration correctness contract" — schema migration rules
"""
